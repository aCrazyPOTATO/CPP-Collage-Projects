#include <iostream>
using namespace std;
#define mul(a, b) a *b;
#define div(a, b) a / b;
int main()
{
    int x = 2, y = 3;
    float n = 5.0, m = 2.0;
    cout << mul(x, y);
    cout << "\n";
    cout << div(n, m);
    return 0;
}